import { FlyLeftDirective } from './fly-left.directive';

describe('FlyLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyLeftDirective();
    expect(directive).toBeTruthy();
  });
});
